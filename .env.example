# Database Configuration
WORDPRESS_DB_HOST=wp_db
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change_me_immediately
WORDPRESS_TABLE_PREFIX=wp_

# PHP & WordPress Configuration
# Note: Memory limit is now managed via php/uploads.ini directly
# but you can still define WP-specific constants here if needed.
WORDPRESS_DEBUG=false
WORDPRESS_CONFIG_EXTRA="define('DISALLOW_FILE_EDIT', true); define('DISABLE_WP_CRON', true);"

# OpenLiteSpeed Configuration
OLS_PASSWORD=admin123

# Adminer & Database Management
# Change this to a secret path for added security
DB_MANAGER_PATH=/wp-db-admin
ADMINER_DESIGN=pepa-linha

# File Manager Configuration
# Change this to a secret path for added security
FILE_MANAGER_USER=admin
FILE_MANAGER_PASSWORD=change_me_immediately
FILE_MANAGER_PATH=/file-manager-secret

# SMTP Relay Configuration
SMTP_SERVER=smtp.mailgun.org
SMTP_PORT=587
SMTP_USERNAME=your_username
SMTP_PASSWORD=your_password
SERVER_HOSTNAME=example.com

# AWS SES Tagging
SES_TENANT_TAG=your-tenant-name
