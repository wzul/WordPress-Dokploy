# msmtp configuration for routing native PHP mail() through the relay sidecar
defaults
auth           off
tls            off
logfile        /var/log/msmtp.log

# The SMTP relay sidecar
account        default
host           mail-relay
port           25
from           wordpress@localhost
